<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand">Memes</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="active">
          <a href="/">Home</a>
        </li>
        <li>
          <a>Gallery</a>
        </li>
    </div>
  </div>
</nav>
<div class="container" role="main">
  <div class="page-header">
    <h3 id="imgTitle"><%= @post.title %></h3>
    <span class="label label-success">Upvotes: <%= @post.votes %></span>
  </div>
  <div class="jumbotron vertical-center">
    <!--Use iframe tag for video link, img tag for image link-->
    <% if @is_video %>
    <iframe class="center" src=<%= @url %> height="700px">iframe not supported</iframe>
    <% else %>
    <%= javascript_pack_tag 'post_index' %>
    <img data-src="holder.js/200x200" id="homeImg" class="img-thumbnail center" alt=<%=h @post.title %> src=<%= @url %> data-holder-rendered="true"><br>
    <!--Modal image for image links-->
    <div id="homeModal" class="modal">
      <img id="modalImg" class="modal-content">
      <div id="caption"><%= @post.title %></div>
    </div>
    <% end %>
  </div>  
  <form action=<%= @link %> method="get" target="_blank">
    <button type="submit" class="btn btn-lg btn-info">Source</button>
  </form>
</div>
